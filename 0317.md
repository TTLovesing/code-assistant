# 2026-03-17 全流程记录（函数补全统一格式 + 重训准备）

## 1. 服务器与磁盘整理

- 远端主目录迁移到数据盘：`/root/code-assistant -> /root/autodl-tmp/code-assistant`（软链接兼容原路径）。
- 清理旧训练与临时评测产物，释放系统盘空间。
- 当前大文件都放在数据盘路径体系下（通过 `/root/code-assistant` 软链接访问）。

## 2. 统一任务标准（先定标准）

- 新建标准文档：`/root/code-assistant/function_generation_standard.md`
- 统一目标：`Python Function Completion`
- 统一约束：
  - Prompt 必须含 `def ...:` 函数骨架
  - Completion 只能是函数体（不含顶层 `def/class`，不含代码围栏）
  - SFT 与 DPO 字段统一到函数补全语义

## 3. 数据转换与验收

### 3.1 转换脚本

- `/root/code-assistant/scripts/convert_to_func_completion.py`

输入：
- `/root/code-assistant/data/processed/sft_train.jsonl`
- `/root/code-assistant/data/processed/dpo_train.jsonl`

输出：
- `/root/code-assistant/data/processed_v2/sft_train_func_completion.jsonl`
- `/root/code-assistant/data/processed_v2/dpo_train_func_completion.jsonl`

转换统计：
- SFT: `50000 -> 49108`
  - `drop_no_function=29`
  - `drop_top_level_def_completion=863`
- DPO: `1490 -> 675`
  - `drop_no_function=7`
  - `drop_name_mismatch=788`
  - `drop_top_level_def_completion=20`

### 3.2 验收脚本

- `/root/code-assistant/scripts/validate_func_completion_format.py`

输出报告：
- `/root/code-assistant/reports/validate_func_completion_report.json`

验收结论：
- `summary.pass = true`
- SFT `concat_parse_ok_ratio = 0.9985`
- DPO `chosen/rejected concat_parse_ok_ratio = 1.0 / 1.0`
- Eval Prompt `prompt_has_def_ratio = 1.0`

## 4. 抽样检查

- 已抽样打印 SFT / DPO 转换后样本，结构符合函数补全（prompt 含 def，answer 为函数体）。
- Eval 使用文件：`/root/code-assistant/eval/results_strict/humaneval_subset.json`
  - 该文件本次未重写（时间早于本次转换）
  - 但格式已满足函数补全评测需求

## 5. 产物索引

- 标准文档：`/root/code-assistant/function_generation_standard.md`
- 转换报告：`/root/code-assistant/reports/convert_func_completion_report.json`
- 验收报告：`/root/code-assistant/reports/validate_func_completion_report.json`
- 汇总说明：`/root/code-assistant/format_conversion_report.md`

## 6. 下一步（本轮执行）

- 将 LLaMA-Factory 数据注册切换到 `processed_v2` 文件名
- 启动新的 SFT 重训（基于统一后的函数补全数据）
- 训练日志：`/root/code-assistant/training/logs/sft_v2.log`

## 7. 本轮实际执行记录（SFT 已启动）

### 7.1 配置切换

- `dataset_info.json` 中：
  - `sft_pyfunc.file_name -> sft_train_func_completion.jsonl`
  - `dpo_pyfunc.file_name -> dpo_train_func_completion.jsonl`
- 数据已拷贝到：
  - `/root/code-assistant/LLaMA-Factory/data/sft_train_func_completion.jsonl`
  - `/root/code-assistant/LLaMA-Factory/data/dpo_train_func_completion.jsonl`

### 7.2 启动异常与修复

- 首次启动失败：`dataset_info.json` 含 UTF-8 BOM，LLaMA-Factory 报 `JSONDecodeError: Unexpected UTF-8 BOM`
- 修复：重新上传无 BOM 的 `dataset_info.json`

### 7.3 SFT 启动命令

```bash
source /root/miniconda3/etc/profile.d/conda.sh
conda activate lf
cd /root/code-assistant/LLaMA-Factory
nohup llamafactory-cli train /root/code-assistant/train_sft_pyfunc.yaml > /root/code-assistant/training/logs/sft_v2.log 2>&1 &
```

### 7.4 当前状态

- 日志确认已读取数据：`Loading dataset sft_train_func_completion.jsonl...`
- 已进入 tokenizer 阶段并显示进度条（49108 样本）
- 训练日志文件：`/root/code-assistant/training/logs/sft_v2.log`

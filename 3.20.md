# 3.20 执行SOP（给另一台电脑上的 Codex）

目标：确保在另一台机器上阅读本文件后，Codex 可以稳定执行，不跑偏。

## A. 环境与固定路径

- 服务器：`root@connect.nmb1.seetacloud.com:37996`
- 项目根目录：`/root/code-assistant`
- 本轮评测目录：`/root/code-assistant/eval/results_reg45_v4_0319`
- 最终结果文件：`/root/code-assistant/eval/results_reg45_v4_0319/leaderboard.json`
- 改进案例输出：`/root/code-assistant/improved_cases_0320.txt`
- 提取脚本：`/root/code-assistant/scripts/extract_improved_cases.py`

## B. 强约束（必须遵守）

1. 先确认评测结束，再写总结。  
2. 没有 `leaderboard.json` 时，禁止写“最终结论”。  
3. 只使用上述固定目录，禁止自行改路径。  
4. 所有新增内容必须可复现（附命令或文件路径）。

## C. 执行步骤

### Step 1: 检查评测是否结束

```bash
cd /root/code-assistant
ps -ef | grep eval_step10.py | grep -v grep || echo EVAL_STOPPED
ls -lah /root/code-assistant/eval/results_reg45_v4_0319/leaderboard.json
```

判定标准：
- 若看到 `leaderboard.json` 存在，进入 Step 2。
- 若文件不存在，等待评测完成后重试。

### Step 2: 将最新评测结果写入 md 文件

需要更新：
- `/root/code-assistant/0319.md`
- `/root/code-assistant/final.md`

至少写入以下信息：
- Base/SFT/DPO 的 `pass@1`、`pass@3`、`preference_win_rate`
- 本轮是否达标
- 推荐模型与原因（1-2 句）

建议命令（先查看结果）：

```bash
cat /root/code-assistant/eval/results_reg45_v4_0319/leaderboard.json
```

### Step 3: 提取“badcase 变好”案例到 txt

执行：

```bash
/root/miniconda3/envs/lf/bin/python /root/code-assistant/scripts/extract_improved_cases.py \
  --results-dir /root/code-assistant/eval/results_reg45_v4_0319 \
  --out-txt /root/code-assistant/improved_cases_0320.txt \
  --max-cases 12
```

验收：

```bash
ls -lah /root/code-assistant/improved_cases_0320.txt
head -n 120 /root/code-assistant/improved_cases_0320.txt
```

必须包含：
- task_id
- 三模型 pass@1 对比
- 具体输出片段（base/sft/dpo）
- improved_by（SFT 或 DPO）

### Step 4: 上传训练后三模型到 GitHub（注意大文件）

推荐方式：GitHub Release 或 Git LFS。  
不建议直接普通 git push 大模型文件。

模型目录：
- `training/sft_ckpt_reg20_v3_0319/`
- `training/dpo_ckpt_reg20_v4b_0319/`
- `models/Qwen2.5-Coder-3B-Instruct/`（如需上传）

最小可交付（若不上传权重文件本体）：
- 在 md 中记录模型目录、大小、哈希、下载方式/外链。

## D. 交付清单

执行完成后应有：

1. `0319.md` 已更新最终结果  
2. `final.md` 已填入本轮最终结果  
3. `improved_cases_0320.txt` 已生成并可读  
4. 模型上传策略已执行（Release/LFS）并写入链接

## E. 失败处理

- 若 `dpo_humaneval.jsonl` 缺失：说明评测未完整结束，返回 Step 1。  
- 若脚本报路径错误：只允许修正为本文件固定路径。  
- 若 GitHub 上传失败：记录报错并改用 Release 方式上传。
